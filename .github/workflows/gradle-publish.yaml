name: Publish on any tag

on:
  push:
    tags:
      - '*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run publish script
        run: echo "Publishing for tag ${{ github.ref_name }}"
